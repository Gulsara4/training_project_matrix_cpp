# Matrix C++


Статическая библиотека для работы с матрицами, реализованная на **C++20** с использованием
объектно-ориентированного подхода и ручного управления памятью.


---

## 🧠 Описание

Библиотека реализует класс `S21Matrix`, предоставляющий базовые и расширенные операции над матрицами:
арифметические операции, вычисление определителя, обратной матрицы и алгебраических дополнений.

---

## ⚙️ Реализованные операции

### Методы класса

- `EqMatrix`
- `SumMatrix`
- `SubMatrix`
- `MulNumber`
- `MulMatrix`
- `Transpose`
- `CalcComplements`
- `Determinant`
- `InverseMatrix`

Все операции проверяют корректность входных данных и выбрасывают исключения
в случае некорректных состояний (размерности, нулевой определитель и т.д.).

**Перегруженные операторы**

Поддерживаются:
- `+`, `-`, `*`
- `+=`, `-=`, `*=`
- `==`
- `=`
- `(int i, int j)` — доступ к элементам матрицы


---

## 🧪 Тестирование

- Полное покрытие unit-тестами (библиотека **Google Test**)

---

## 🛠 Сборка

Сборка осуществляется с помощью `Makefile`.

Доступные цели:
```bash
make all
make clean
make test
make s21_matrix_oop.a
